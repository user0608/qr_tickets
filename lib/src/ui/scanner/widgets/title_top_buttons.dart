import 'package:flutter/material.dart';

class TitleTopButtons extends StatelessWidget {
  const TitleTopButtons({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Icon(Icons.drag_handle, color: Colors.white));
  }
}
